# dft — scan + ATPG (Fault)

Scan-chain insertion + ATPG scripts (Fault). Test mode muxed onto `uio` under a
test-enable CSR bit. Target **≥95% stuck-at** coverage on scanned logic; report the
real number. Lands in **Phase 6**.
