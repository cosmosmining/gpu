# WarpOne regression list — `make regress` runs these.
# Format:  <suite>/<entry>   # description
#
# Phase 1 (active):
compliance/test_compliance.py    # 12 directed P0 ISA-compliance programs on the simulator
#
# Phase 2+ (added as they land):
#   lockstep/*    RTL-vs-sim retired-state comparison kernels
#   directed/*    divergence depths 0-4, all-lanes-inactive, stack over/underflow,
#                 barrier deadlock, every bank-conflict pattern
